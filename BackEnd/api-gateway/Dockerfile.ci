FROM eclipse-temurin:17-jre

WORKDIR /app

COPY target/*.jar /tmp/
RUN JAR_FILE=$(ls /tmp/*.jar | grep -v '\.original$' | head -n 1) \
    && test -n "$JAR_FILE" \
    && cp "$JAR_FILE" /app/app.jar

EXPOSE 8083

ENTRYPOINT ["java","-jar","/app/app.jar"]
